<div class="test test_2">
  <h2 class="text_alignment text_alignment_2">Add Biz to Bizlist</h2><span id="vert"></span>
    <%= form_for [user, bizlist] do |f| %>
    <%= f.collection_select :add_to, Business.all, :id, :name %>
      <%= f.submit %>
    <% end %>
</div>
